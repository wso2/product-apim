<% jagg.template("layout/json-editor", function(inputs, outputs, jagg) { %>
    <div class="row-fluid">
        <% if(outputs.top) { %>
            <% jagg.includeBlocks(outputs.top); %>
        <% } %>
            <% jagg.includeBlocks(outputs.middle); %>

    </div>

<% }); %>