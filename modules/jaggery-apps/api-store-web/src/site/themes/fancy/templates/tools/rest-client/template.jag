<% jagg.template("tools/rest-client", function(inputs, outputs, jagg) { %>
    <div class="tryit-content" id="tryit-content">
        <% jagg.includeBlock("api/tryit", outputs); %>
    </div>
<% }); %>