package $package;

function getUrlFromKey (string key) (string){
    map endpointMap = {};
#foreach($endpoint in $endpoints)
    endpointMap["$endpoint.name"] = "$endpoint.endpointConfig";
#end
    string url = endpointMap[key];
    return url;
}