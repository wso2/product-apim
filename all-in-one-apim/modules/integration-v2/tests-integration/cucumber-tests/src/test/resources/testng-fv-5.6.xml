<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<!--
  Isolated verification lane (Phase 5.6): provisioning targets the block's OWN container under parallelism.
  Suite parallel="tests" thread-count="2" => both blocks (and their two REAL APIM containers) boot and
  provision simultaneously. Both blocks set initTenantUsers=true with the DEFAULT tenant set, so each
  provisions the SAME tenant domains (carbon.super + tenant1.com + users testUser1/testUser11) - a wrong-URL
  bug (a provisioner hitting the other block's container, or shared-scope crosstalk under the
  suiteName::testName namespace) only manifests when two blocks provision the same names at once. Each runs
  BlockProvisioningProbeRunner, whose scenario SOAP-retrieves tenants/users from ITS OWN block's baseUrl and
  asserts they exist there, then records an observation (container id + baseUrl). Both share the docker label
  block=fv-5.6 (single cleanup filter) but have distinct <test> names (distinct shared scopes).
-->
<suite name="Framework Verification 5.6" parallel="tests" thread-count="2">
    <listeners>
        <listener class-name="org.wso2.am.integration.cucumbertests.utils.listeners.BlockLifecycleListener"/>
        <listener class-name="org.wso2.am.integration.cucumbertests.utils.listeners.BlockScopeListener"/>
    </listeners>
    <test name="Phase5.6-BlockA">
        <parameter name="blockLabel" value="fv-5.6"/>
        <parameter name="initTenantUsers" value="true"/>
        <classes>
            <class name="org.wso2.am.integration.cucumbertests.runners.block.BlockProvisioningProbeRunner"/>
        </classes>
    </test>
    <test name="Phase5.6-BlockB">
        <parameter name="blockLabel" value="fv-5.6"/>
        <parameter name="initTenantUsers" value="true"/>
        <classes>
            <class name="org.wso2.am.integration.cucumbertests.runners.block.BlockProvisioningProbeRunner"/>
        </classes>
    </test>
</suite>
