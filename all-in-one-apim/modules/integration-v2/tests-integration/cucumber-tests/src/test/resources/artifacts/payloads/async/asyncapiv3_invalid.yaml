asyncapi: 3.0.0
info:
  title: InvalidChannelRefAsyncAPI
  version: 1.0.0
channels:
  user/signedup:
    address: user/signedup
    messages:
      UserSignedUp:
        $ref: '#/components/messages/UserSignedUp'
operations:
  UserSignedUp:
    action: receive
    channel:
      $ref: '#/channels/nonexistent'
components:
  messages:
    UserSignedUp:
      payload:
        type: object
