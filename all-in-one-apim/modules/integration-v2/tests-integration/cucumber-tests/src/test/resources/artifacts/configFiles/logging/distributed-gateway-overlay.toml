# Distributed APIM component overlay for the logging block. The restart API is exposed by
# both the Control Plane and Gateway components, which the logging feature restarts to verify
# persisted configuration in their respective log streams.
[server]
enable_restart_from_api = true
